---
description: Environment & Configuration
globs: **/*.{ts,tsx,js,jsx}
alwaysApply: true
---
Load configs from .env.local, using UPPERCASE for vars (e.g., NEXT_PUBLIC_SUPABASE_URL).

Never hardcode secrets or URLs in code.

Use Prettier for formatting; follow Cursor's tab/space conventions.